# Web API
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0
pydantic-settings>=2.6.0

# Anthropic SDK — used with MiniMax's Anthropic-compatible endpoint
anthropic>=0.40.0

# HTTP client (kept for other uses)
httpx>=0.27.0

# Supabase (Storage + optional Postgres updates)
supabase>=2.10.0

# PDF: native text layer first, then OCR fallback
pymupdf>=1.24.0
pytesseract>=0.3.13
pdf2image>=1.17.0
Pillow>=11.0.0

# Env
python-dotenv>=1.0.0
